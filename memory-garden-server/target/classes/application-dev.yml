spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://localhost:3306/memory_garden?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:lujun7850}

# JWT 密钥（开发环境默认值，生产环境必须通过环境变量 JWT_SECRET 注入强随机密钥）
jwt:
  secret: ${JWT_SECRET:dev-only-secret-key-do-not-use-in-production-2026}

logging:
  level:
    com.memorygarden: debug
